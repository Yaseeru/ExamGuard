[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm ci --production"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && node server.js"

[variables]
NODE_ENV = "production"
